[project]
name = "smv-gsd-mtm-tool"
version = "0.1.0"
description = "Calculator for apparel SMV, operation breakdown, and thread consumption using GSD/MTM codes"
readme = "README.md"
requires-python = ">=3.10"
authors = [{name = "Codex"}]

[project.scripts]
smv-tool = "smv_tool.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
